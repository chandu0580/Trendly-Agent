# Web layer
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.9,<3.0
python-dotenv>=1.0,<2.0

# Orchestration. The provider is any OpenAI-compatible endpoint, so a free tier
# (Google AI Studio, or a self-hosted gateway) works without code changes.
langgraph>=1.0,<2.0
langchain-core>=1.0,<2.0
langchain-openai>=0.3,<1.0
# Not imported directly — langchain-openai talks to the provider through it.
# Pinned to the major the suite was verified against: langchain-openai leaves it
# open, so a fresh install would otherwise take openai 2.x and deploy a stack no
# test has run on.
openai>=1.40,<2.0

# Policy retrieval. Chroma runs locally and persists to disk; its bundled ONNX
# MiniLM embedding needs no API key and no torch.
chromadb>=1.0,<2.0

# Tests. httpx backs fastapi.testclient.TestClient.
pytest>=8.0,<9.0
httpx>=0.27,<1.0
